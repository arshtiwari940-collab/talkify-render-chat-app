# Required when deploying the frontend to Vercel (backend hosted separately).
# Use your Render/Railway/Fly backend URL — no trailing slash.
VITE_API_URL=https://your-backend.onrender.com
VITE_SOCKET_URL=https://your-backend.onrender.com
